on:
  push:
    branches-ignore:
      - master
      - ghpages

name: "check lintr"

jobs:
  check-package:
    runs-on: ubuntu-latest
    name: "check lintr"
    steps:
      - uses: inbo/actions/lint_pkg@master
